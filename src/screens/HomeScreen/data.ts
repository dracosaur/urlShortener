export function useHomeScreen() {
  const title = 'Encurtador de URLs';
  const subtitle =
    'Cole a URL abaixo para gerar um link curto e acompanhe o histórico recente.';

  return {
    title,
    subtitle,
  };
}
